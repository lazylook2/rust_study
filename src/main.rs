#![allow(unused_variables)]
#[allow(unused_mut)]
use rust_study::error::e2;
use rust_study::generics_trait::{generics, traits, lifetime};
use rust_study::iterators_closures::Iterator;
use rust_study::pointers::box1;

fn main() {
    box1();
}
